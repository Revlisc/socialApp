import { makeStyles } from '@material-ui/styles';

export default makeStyles(() => ({
  form: {
      maxWidth: '540px',
      width: '80%',
      display: 'flex',
      alignItems: 'center',
      margin: '0 auto',
      
  }
}));